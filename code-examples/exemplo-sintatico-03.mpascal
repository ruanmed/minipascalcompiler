! SINTÁTICO 3 - Programa de teste com declaração de variáveis - Sintaxe válida e contexto inválido 
program test;

var a, b, c: integer;
var d, e, j, y, u: real;
var x : array[10 .. 20] of integer;
var h, n: boolean;

begin
	if (a or b)  then
		begin
			x := y;
			h := a;
		end
	else
		begin
			a:=b;
			d:=e;
			a:=d;
			d:=c;
			y:=x;
			x:=y;
			y[1]:=a;
			while (x < (y+a+j))
			do
			x:=a;
		end
	;
begin
end;

end.