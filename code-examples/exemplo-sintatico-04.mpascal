! SINTÁTICO 4 - PROGRAMA COM SINTAXE INVÁLIDA
program test;

var a, b, c: real;
var x : array[10. .. 20] of integer;

begin
	if (a or b)  then
		begin
			x := y;
			h := a;
		end
	else
		begin
			a:=b;
			d:=e;
			a:=d;
		end
	;
begin
end;

end.